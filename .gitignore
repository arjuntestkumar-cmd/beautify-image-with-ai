.env
.data/
__pycache__/
*.pyc
.venv/
venv/

# Model weights are large binaries — keep them out of git.
models/*.pth
gfpgan/weights/*.pth

# cloudflared binary (52 MB) - go-public.ps1 fetches it on demand
tools/
